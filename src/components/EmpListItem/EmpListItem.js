/*
import React from 'react';
import {style} from './EmpListItemStyle';

export const EmpListItem = () => {

  return(
    <ListData>
      <ListItem w={130}>{emp.username}</ListItem>
      <ListItem w={140}>{emp.name}</ListItem>
      <ListItem w={210}>{emp.email}</ListItem>
      <ListItem w={90}>{emp.depId}</ListItem>
      <ListItem w={200}>{emp.position}</ListItem>
      <ListItem w={70}>{emp.createdAt}</ListItem>
      <ListItem w={100}>
        <BtnLayout w={50} value="detailBtn">보기</BtnLayout>
      </ListItem>
    </ListData>
  );
};

const {ListData, ListItem, BtnLayout} = style;

// EmpListItem.propTypes = {
//   emp: PropTypes.arrayOf(
//     PropTypes.objectOf(
//       PropTypes.oneOfType([PropTypes.number, PropTypes.string, PropTypes.bool])
//     )
//   ).isRequired,
// };
*/
